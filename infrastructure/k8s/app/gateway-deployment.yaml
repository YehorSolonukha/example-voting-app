apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  labels:
    app: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
      - name: gateway
        image: yehor542/gateway:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 9090
        env:
        - name: WAF_DB_USER
          valueFrom:
            secretKeyRef:
              name: waf-db-cluster-app
              key: username
        - name: WAF_DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: waf-db-cluster-app
              key: password
        - name: WAF_DB_URL
          value: postgres://$(WAF_DB_USER):$(WAF_DB_PASSWORD)@waf-db-cluster-rw:5432/waf_logs
        - name: WAF_REDIS_URL
          value: redis://waf-redis-ha-master:6379
        - name: VOTE_APP_URL
          value: http://vote

apiVersion: v1
kind: Service
metadata:
  name: waf
  labels:
    app: waf
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  ports:
  - port: 9090
    targetPort: 9090
    nodePort: 30090
  selector:
    app: waf

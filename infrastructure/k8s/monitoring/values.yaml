grafana:
  adminUser: admin
  adminPassword: prom-operator

  service:
    type: LoadBalancer

prometheus:
  prometheusSpec:
    retention: 3d
    resources:
      requests:
        cpu: 200m
        memory: 512Mi
      limits:
        cpu: 500m
        memory: 1Gi

alertmanager:
  enabled: false
